repositories {
    flatDir {
        dirs 'libs'
    }
}

// Apply values after project's build-extras.gradle
cdvPluginPostBuildExtras.add({
    dependencies {
        compile(name:'usage-plugin-4.0.2', ext:'aar')
        compile(name:'Usage-3.16.10', ext:'aar')
    }
})